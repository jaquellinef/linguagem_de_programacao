#include <stdio.h>

#include <stdlib.h>

#include <locale.h>

int main()

{

	setlocale(LC_ALL, "Portuguese");

	float vl_compra, desc, vl_pagar;

		printf("\n Digite o valor da compra: ");

	scanf ("%f", &vl_compra);

	if (vl_compra <=30){

		desc=0;

		vl_pagar = vl_compra - desc;

		printf ("\n Desconto é: %.2f", desc);

		printf ("\n Valor a pagar é: %.2f", vl_pagar);

	}else if (vl_compra <=100){

		desc=vl_compra * 0.05;

		vl_pagar = vl_compra - desc;

		printf ("\n Desconto é: %.2f", desc);

		printf ("\n Valor a pagar é: %.2f", vl_pagar);

	}else if (vl_compra <=250){

		desc=vl_compra * 0.10;

		vl_pagar = vl_compra - desc;

		printf ("\n Desconto é: %.2f", desc);

		printf ("\n Valor a pagar é: %.2f", vl_pagar);

	}else{

		desc=vl_compra * 0.15;

		vl_pagar = vl_compra - desc;

		printf ("\n Desconto é: %.2f", desc);

		printf ("\n Valor a pagar é: %.2f", vl_pagar);

	}		

}

 
